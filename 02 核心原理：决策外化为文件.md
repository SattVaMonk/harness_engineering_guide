# 核心原理：决策外化为文件

## 一句话总结

> **决策外化为文件 = 给无状态的 LLM 配一块持久化硬盘。它把"易失的对话记忆"升级为"可寻址、可版本化、可团队共享的真相源"，从根本上解决了 Context Rot 的核心成因——决策遗忘、上下文稀释、协作不一致。**

这是 Harness Engineering 最深刻的洞察：**不是去对抗 LLM 的局限，而是在它周围搭建工程化基础设施，让局限变得无关紧要**。

---

## 一、问题的本质：LLM 是无状态的

LLM 的对话**只在当前上下文窗口内有"记忆"**。每一次新会话（或上下文被压缩、清空）后，所有讨论过的决策——架构选型、命名约定、为什么不用某个方案、踩过的坑——都会**完全消失**。

这导致三个具体问题：

### 问题 1：决策遗忘（Decision Amnesia）

**症状**：
```
第一周：你和 AI 讨论了 30 分钟，决定用 PostgreSQL 而不是 MongoDB
       理由：需要事务支持 + 团队熟悉 SQL
第二周（新会话）：AI 看到代码后建议"这个场景应该用 MongoDB 更合适"
```

**根因**：决策的**结论**被代码反映，但决策的**理由**没有任何载体保存。AI 只能看到现状，看不到"为什么是这样"。

### 问题 2：上下文稀释（Context Dilution）

**症状**：在长对话中，最早确立的关键约束（"所有 API 必须经过 JWT 中间件"）随着对话推进被埋在历史消息中，AI 注意力机制对早期 token 的权重逐渐降低，最终输出会"忘记"这些约束。

**研究背景**：在 100K+ tokens 的上下文中，模型对中间部分信息的召回率显著下降（"Lost in the Middle" 现象，Stanford/Berkeley 2023 年论文）。

### 问题 3：协作不一致（Multi-Agent Divergence）

**症状**：当多个 AI 代理（或多个开发者+AI）并行工作时，每个代理都在自己的上下文中做决策，导致：
- A 代理写的接口用 camelCase，B 代理写的用 snake_case
- A 代理认为该用 REST，B 代理觉得该用 gRPC
- 没有"权威源头"裁决

---

## 二、原理：用文件系统替代对话记忆

**核心机制**：把决策从"易失的对话记忆"迁移到"持久的文件系统"。

### 文件系统的五个关键属性对比

| 属性 | 对话记忆 | 文件系统 |
|------|---------|---------|
| **持久性** | 会话结束即消失 | 永久保存 |
| **可寻址** | 只能线性回溯 | 通过路径精确读取 |
| **可索引** | 注意力权重衰减 | 按需加载，权重恒定 |
| **可版本化** | 无法对比变化 | Git 完整追踪 |
| **可共享** | 私有于会话 | 团队共享真相源 |

### 这本质上是什么？

**这是把 LLM 改造成了一种"图灵机"**：
- LLM 本身 = CPU（无状态计算单元）
- 上下文窗口 = 寄存器（短期工作内存）
- **文件系统 = 硬盘（长期记忆）**

经典计算机架构通过"寄存器+内存+硬盘"的层级存储解决了"CPU 是无状态"的问题。Harness Engineering 在做同样的事：用文件系统给 LLM 配上一个外部硬盘。

---

## 三、决策外化的三个层次

不同 Harness 框架在不同层次做了文件外化：

### 层次 1：原则外化（Constitution / 宪法）

**外化对象**：跨功能、跨时间的全局约束

**典型文件**：
- `constitution.md`（Spec-Kit）
- `CLAUDE.md` / `AGENTS.md`（通用约定）
- `PROJECT.md`（GSD）

**示例**：
```markdown
# constitution.md
- 所有 API 必须 < 200ms
- 禁止引入新的状态管理库
- 用户密码必须 bcrypt 加密
```

**解决问题**：每个新会话开始时，AI 自动加载这些约束，**绝不会遗忘**。

### 层次 2：决策记录外化（Decision Log）

**外化对象**：某个具体决策的"为什么"

**典型文件**：
- `proposal.md`（OpenSpec）
- ADR（Architecture Decision Record）
- `CONTEXT.md`（GSD）

**示例**：
```markdown
# proposal.md
## 动机
为什么选 Kafka 而不是 RabbitMQ？
- 需要支持 1000 万级别消息吞吐
- 需要消息回溯能力
- 团队已有 Kafka 运维经验

## 影响范围
- user-service: 新增 Producer
- product-service: 新增 Consumer

## 风险
- Kafka 集群运维成本
```

**解决问题**：3 个月后回看时，**决策的理由不会丢失**。AI 在做后续修改时会读到这些上下文，避免做出与原决策冲突的建议。

### 层次 3：状态外化（State / 进度）

**外化对象**：当前正在做什么、做到哪一步

**典型文件**：
- `tasks.md`（带 checkbox 状态）
- `STATE.md`（GSD）
- `ROADMAP.md`

**示例**：
```markdown
# tasks.md
- [x] Task 1: 创建数据表
- [x] Task 2: 实现 Service 层
- [ ] Task 3: 实现 API 路由 ← 当前进行
- [ ] Task 4: 编写测试
```

**解决问题**：会话中断后重启，AI 读取 `tasks.md` 立刻知道**接下来做什么**，无需用户重新讲述。

---

## 四、为什么"外化为文件"比"塞进上下文"更好？

很多人会想：那我每次都把所有决策塞到 prompt 里不就行了？

**为什么不行**——三个根本原因：

### 1. 上下文窗口是有限且昂贵的

塞所有历史 = token 成本暴涨 + 窗口溢出。
文件外化是**按需加载**：AI 只读取当前任务相关的文件。

### 2. 注意力会被稀释

即便所有信息塞得下，**模型对长上下文中部信息的召回率会显著下降**（"Lost in the Middle" 现象）。

文件系统通过**显式引用**（"请阅读 constitution.md"）让信息成为**当前焦点**，而不是埋在 100K token 的中间。

### 3. 文件可被人类和 AI 共同维护

```
对话历史 → 只有 AI 能"读懂" → 不可审计
文件系统 → 人类也能编辑 → 可 Code Review、可 Git 追踪、可团队共享
```

这创造了**人机协作的共同基底**——人改了一行 constitution，下次 AI 立刻遵守新规则。

---

## 五、各 Harness 框架对"决策外化"的具体实践

| 框架 | 外化策略 | 具体文件 |
|------|---------|---------|
| **Spec-Kit** | 全量外化（一次性写完整规范） | `constitution.md` + `spec.md` + `plan.md` + `tasks.md` |
| **OpenSpec** | 增量外化（每次变更都新建 change） | `changes/<change>/proposal.md` + `specs/*` 增量 |
| **GSD** | 状态机外化（强调上下文隔离） | `PROJECT.md` + `CONTEXT.md` + `STATE.md` + `ROADMAP.md` |
| **Trellis** | 项目记忆外化（解决跨会话失忆） | `.trellis/spec/` + `.trellis/tasks/` |

---

## 六、什么时候不该外化？

不是所有信息都该外化。**临时决策**和**实现细节**外化反而是负担：

| 应该外化 | 不该外化 |
|---------|---------|
| 跨功能的架构原则 | 某次单元测试的临时调试 |
| 多人共识的命名约定 | 一次性脚本的 if-else 选择 |
| 影响多个模块的重大决策 | 函数内部的局部逻辑 |
| 需要长期遵守的约束 | 已被代码本身充分表达的事实 |

**判断标准**（任意一个为"是"就该外化）：
- 这个决策**未来还会被引用**吗？
- 这个决策的**理由**和**结论**一样重要吗？
- 这个决策需要**多人共识**吗？

---

## 七、延伸思考

### 决策外化与软件工程经典实践的呼应

这并非全新概念，而是经典软件工程实践在 AI 时代的延续：

| 经典实践 | Harness 时代的对应物 |
|---------|---------------------|
| ADR（架构决策记录） | proposal.md / CONTEXT.md |
| README.md / CONTRIBUTING.md | constitution.md / CLAUDE.md |
| Issue Tracker | tasks.md / STATE.md |
| Code Review | proposal.md 的团队评审 |

**区别**：经典实践服务于人类协作，Harness 把**同一套机制扩展为人机协作的共同基础设施**。

### 与"流程结构化"和"任务原子化"的关系

Harness 解决 Context Rot 有三个互补策略：

```
决策外化为文件        → 解决"记忆"问题（What/Why 不丢失）
流程结构化为阶段      → 解决"顺序"问题（每阶段聚焦）
任务原子化为单元      → 解决"窗口"问题（每任务独立上下文）
```

三者共同构成完整的"工程化笼子"。**决策外化是基础**——没有它，后两者也无从发挥。

---

## 参考资源

- [GSD 框架实战：解决 AI 编程的 context rot 问题](https://blog.zhijun.io/posts/gsd-get-shit-done-project-framework)
- [CSDN - 深度解析 GSD 上下文工程](https://blog.csdn.net/weixin_45888077/article/details/159474157)
- [Lost in the Middle: How Language Models Use Long Contexts (Stanford/Berkeley 论文)](https://arxiv.org/abs/2307.03172)
- [掘金 - 6 个代表性 AI 编程 Harness 工程化框架拆解](https://article.juejin.cn/post/7626998085928189998)
- [CSDN - AI 大模型-4：Harness（驱动工程）](https://guisu.blog.csdn.net/article/details/159579369)
- [Architecture Decision Records (ADR) - GitHub](https://adr.github.io/)