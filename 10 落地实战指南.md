# 落地实战指南

## 引子：从知识到肌肉记忆

到目前为止你已经：
- ✅ 理解了 Harness Engineering 是什么
- ✅ 掌握了三大核心原理
- ✅ 深入了三类代表框架
- ✅ 学会了多框架组合策略

但**知识不等于能力**。真正的考验是：在你的项目、你的团队、你的现实约束下，能不能让这套方法跑起来？

本文档解决三个最棘手的实际问题：
1. **怎么开始**——从零搭建到生产可用的完整路径
2. **怎么推行**——让团队从抵触到主动用的策略
3. **怎么救火**——常见问题诊断与解决

---

## 第一部分：从零落地

### 自我评估

落地前先问自己 7 个问题：

#### 问题 1：你的项目类型？
- [ ] 全新项目（绿地）
- [ ] 已有项目（棕地）
- [ ] 多个仓库（微服务）
- [ ] 单一仓库（单体）

#### 问题 2：你的团队规模？
- [ ] 单人开发
- [ ] 2-5 人小团队
- [ ] 6-20 人中等团队
- [ ] 20+ 人大团队

#### 问题 3：你的项目周期？
- [ ] 短期（< 1 个月）
- [ ] 中期（1-6 个月）
- [ ] 长期（> 6 个月）

#### 问题 4：你的 AI 工具栈？
- [ ] Claude Code
- [ ] Cursor
- [ ] GitHub Copilot
- [ ] 多种混合

#### 问题 5：团队对 AI 编程的接受度？
- [ ] 高（已在用）
- [ ] 中（部分人用）
- [ ] 低（基本不用）
- [ ] 抵触（认为 AI 没用）

#### 问题 6：项目质量要求？
- [ ] 极高（金融、医疗等）
- [ ] 高（生产系统）
- [ ] 中（内部系统）
- [ ] 低（原型/工具）

#### 问题 7：你的角色？
- [ ] 技术负责人（能定规则）
- [ ] 资深开发（有影响力但需说服）
- [ ] 普通开发（只能管自己）
- [ ] 团队负责人（管流程）

### 评估结果 → 推荐起点

| 你的情况 | 推荐起点 |
|---------|---------|
| 单人 + 短期 + 任意项目 | 纯 ECC（轻量增强） |
| 小团队 + 中期 + 棕地 | OpenSpec + ECC |
| 中团队 + 长期 + 任意 | OpenSpec + GSD + ECC（完整组合） |
| 大团队 + 长期 + 绿地 | Spec-Kit + GSD + ECC |
| 团队抵触 + 你只是普通开发 | 自己用 ECC，先证明价值 |

---

### 搭建基础设施

#### Step 1: 准备工具链

**必备**：
```bash
# 1. AI 编辑器（推荐 Claude Code）
# 下载：https://claude.com/claude-code

# 2. Git（必须，所有 Harness 框架依赖）
git --version

# 3. Node.js（OpenSpec、ECC 都需要）
node --version  # >= 18

# 4. uv（Spec-Kit 需要）
curl -LsSf https://astral.sh/uv/install.sh | sh
```

**推荐**：
```bash
# MCP（Model Context Protocol）
# 用于跨仓库读取、外部服务集成
# 详情：https://modelcontextprotocol.io
```

#### Step 2: 创建第一个 CLAUDE.md

无论用哪个组合，都从这个文件开始——它是所有 Harness 的基石。

```bash
# 在项目根目录创建
touch CLAUDE.md
```

**最小可行内容**（30 行起步）：

```markdown
# 项目说明

## 项目背景
[一句话描述这个项目做什么]

## 技术栈
- 语言：[TypeScript/Go/Python/...]
- 框架：[React/Express/...]
- 数据库：[PostgreSQL/MongoDB/...]

## 编码规范
- 命名：[camelCase / snake_case / ...]
- 测试：[Jest / Vitest / ...]
- Lint：[ESLint / Prettier 配置]

## 关键约束
- [约束 1，例如：所有 API 必须经过 auth middleware]
- [约束 2，例如：禁用 any 类型]
- [约束 3，例如：提交前必须通过 lint]

## 当前阶段
- 项目状态：[开发中 / 维护期 / ...]
- 重点：[当前关注的问题]

## AI 工作偏好
- 写代码前先看相关现有代码
- 不要主动重构无关代码
- 复杂修改先描述方案再实施
```

**为什么这个文件这么重要？**

它是**最低成本的 Harness**——即使你不用任何框架，CLAUDE.md 也能让 AI 大幅提升表现。

#### Step 3: 安装核心框架

**起步推荐：OpenSpec + ECC**

```bash
# 1. 安装 OpenSpec
npm install -g @fission-ai/openspec@latest
cd your-project
openspec init

# 2. 接入 ECC（作为 git submodule 或克隆到 .claude/ 目录）
git submodule add https://github.com/everything-claude-code/everything-claude-code.git .claude-tools
```

#### Step 4: 配置 Hooks



Hooks 是**让 Harness 自动生效**的关键。

**最小集合**：

```bash
# .claude-tools/hooks/pre-commit
#!/bin/bash
# 提交前自动检查

# 1. Lint 检查
npm run lint || { echo "Lint failed"; exit 1; }

# 2. 类型检查
npm run typecheck || { echo "Type check failed"; exit 1; }

# 3. 单元测试
npm test || { echo "Tests failed"; exit 1; }

# 4. 敏感信息扫描
if git diff --cached | grep -E "(api_key|password|secret).*=.*['\"]"; then
  echo "Possible secret detected!"
  exit 1
fi
```

**进阶集合**：

```bash
# .claude-tools/hooks/post-edit
#!/bin/bash
# 文件编辑后自动安全扫描

if [[ $EDITED_FILE =~ \.(ts|js|py)$ ]]; then
  # 调用 ECC 的 security-reviewer
  claude-agent invoke security-reviewer \
    --target $EDITED_FILE \
    --mode quick
fi
```

#### Step 5: 第一次试用

不要一上来就用大功能验证。**选一个最小的真实需求**，比如：

> "添加一个用户密码强度检查功能"

走完整流程：
```bash
/opsx:propose 添加密码强度检查（包含长度、大小写、数字、特殊字符）
# 审阅 AI 生成的 proposal、specs、design、tasks

/opsx:apply
# 看 AI 如何按 tasks 执行

/opsx:archive
# 看 Delta 如何合并到主规范
```

**完成后回顾**：
- 哪些步骤让你觉得有价值？
- 哪些步骤让你觉得多余？
- AI 生成的产物质量如何？

---

### 建立工作流

#### 第二周：固化日常流程

**目标**：让 OpenSpec + ECC 成为你的默认工作方式

**要做的**：
1. 任何**新功能**必须用 `/opsx:propose` 启动
2. 任何**bug 修复 > 30 行**必须有 propose
3. 团队中至少一个人和你一起用（哪怕一周一次结对）
4. 周五做一次回顾：本周用 Harness 做了几次？哪些做得好？

**不要做的**：
- 不要追求"所有变更都走流程"——小修改可以豁免
- 不要在团队反对时强推——先用自己的产出说话
- 不要把不相关的工具都加上——专注核心

#### 第三-四周：建立宪法

经过 2 周的实践，你应该开始能识别项目中的"隐含规则"。把它们外化为 constitution（如果用 Spec-Kit）或丰富 CLAUDE.md（如果用 OpenSpec）。

**示例**：

```markdown
# CLAUDE.md（更新后）

## 项目背景
（保持不变）

## 技术栈
（保持不变）

## 编码规范
（更详细化）

### TypeScript 规范
- 严禁使用 `any`，必要时用 `unknown`
- 公共函数必须有 JSDoc 注释
- 错误处理统一用 Result 类型而非 throw

### React 规范
- 函数组件优先，禁用 class 组件
- 状态管理：本地状态用 useState，跨组件用 React Query
- 禁止直接修改 state，必须用 setter

## 项目结构约定
- src/services/: 业务逻辑
- src/hooks/: 自定义 React hooks
- src/components/: UI 组件
- src/utils/: 工具函数（必须无副作用）

## API 设计原则
- RESTful 风格
- URL 版本化：/api/v1/...
- 错误响应统一格式：{ error: { code, message, details } }
- 所有列表接口必须分页

## AI 协作偏好
- 修改文件前先 read 整个文件
- 写新函数前先看是否有现有的类似函数
- 复杂功能先在 design.md 中描述方案
- 不要随意引入新依赖
```

**关键点**：CLAUDE.md 是**活文档**，每周都应该有更新。

---

### 扩展能力

#### 引入更多 ECC Agents

第一个月只用核心 Agent（coder、reviewer），第二个月可以引入更多：

| Agent | 适用场景 |
|-------|---------|
| `security-reviewer` | 涉及认证、权限、敏感数据时 |
| `tdd-tester` | 写新功能时的测试先行 |
| `build-error-resolver` | 编译错误时 |
| `performance-analyzer` | 性能问题诊断时 |
| `database-designer` | 数据模型设计时 |

**实操方式**：
```bash
# 在执行任务时主动调用
@security-reviewer 评估这个登录逻辑

@tdd-tester 先为这个新功能写测试
```

#### 引入 GSD（如果项目变大）

如果你的项目进入"大功能"阶段（涉及多个模块、多人协作），考虑引入 GSD。

**引入信号**：
- 一个变更涉及 > 10 个任务
- 多人需要并行开发同一功能
- 项目周期 > 3 个月
- 频繁出现"上下文不够"的问题

**引入步骤**：
```bash
npx get-shit-done-cc@latest
/gsd-init-existing-project

# 把现有 OpenSpec 变更导入 GSD
/gsd-import-from-openspec changes/your-change
```

---

### 达到成熟

到这一步，你应该已经能：

- [ ] 任何变更都自然走 propose → apply → archive
- [ ] CLAUDE.md 已经积累了 50+ 行项目知识
- [ ] Hooks 自动跑 lint、test、安全扫描
- [ ] 至少 3 个 ECC Agent 是日常使用的
- [ ] 团队中至少 2 人主动使用
- [ ] 已经有 1-2 次因 Harness 避免的事故

如果以上达成 70% 以上，恭喜——**你已经把 Harness 真正落地了**。

---

## 第二部分：团队推行策略

### 推行的核心矛盾



推行 Harness 的最大障碍**不是技术**，而是**人的心理**：

| 团队成员心理 | 真实想法 |
|------------|---------|
| "感觉太繁琐了" | 担心增加自己的工作量 |
| "AI 哪有这么靠谱" | 之前用过 AI 翻车，留下阴影 |
| "我们的项目用不上" | 不愿走出舒适区 |
| "等技术再成熟点" | 拖延的借口 |

应对这些心理需要**策略**，而不是单纯的技术说服。

---

### 策略 1：先证明，再推广

**反模式**：开会说"我们要全员推行 Harness"。

**正模式**：自己默默用一个月，然后展示成果：
- "这个功能我用 OpenSpec 做的，3 天完成，比预估快 2 天"
- "这个 bug 是 ECC 的 security-reviewer 提前发现的，避免了上线事故"
- "这个重构我用 GSD 做的，自动并行了 8 个子任务"

**真实数据胜过任何 PPT**。

---

### 策略 2：找盟友，不找对手



团队中通常有三类人：

| 类型 | 特征 | 推行策略 |
|------|------|---------|
| **早期采用者**（10-20%） | 喜欢新技术，主动尝试 | 一起试点，让他们成为传播者 |
| **观望者**（60-70%） | 看别人怎么样再决定 | 用早期采用者的成功打动他们 |
| **抵触者**（10-20%） | 习惯老方法，认为新东西没价值 | 不强求，等氛围成熟后他们会被裹挟 |

**关键**：找到 2-3 个早期采用者，把他们变成盟友。**不要试图说服抵触者**，浪费精力。

---

### 策略 3：从痛点切入

不要从"这个工具有多先进"切入，而是从"它能解决什么具体痛点"切入。

**痛点对照表**：

| 团队痛点 | Harness 解决方案 |
|---------|----------------|
| "AI 写的代码经常不符合规范" | CLAUDE.md + Hooks |
| "重复造轮子，不知道有没有现成的" | ECC 的 code-explorer Agent |
| "Code Review 找不到深层问题" | ECC 的 security-reviewer + AgentShield |
| "新人上手慢，不熟悉项目约定" | constitution.md / CLAUDE.md |
| "复杂功能 AI 中途就糊涂了" | GSD 的上下文管理 |
| "需求总是改，跟不上" | OpenSpec 的 Delta 模式 |
| "code review 时不知道为什么这么写的" | OpenSpec 的 proposal.md |

**话术**：
> "你最近不是抱怨 AI 写的代码不符合我们的命名规范吗？我用 CLAUDE.md 解决了，要不要试试？"

---

### 策略 4：渐进式接入



**反模式**：一次性引入完整组合（OpenSpec + GSD + ECC + ...）

**正模式**：分 4 个阶段，每个阶段 2-4 周：

#### 阶段 1（第 1-2 周）：CLAUDE.md
- 只让团队写一个 CLAUDE.md
- 不引入任何其他工具
- 让团队感受到"AI 突然听话了"

**门槛**：极低
**价值**：立即可见
**接受度**：90%+

#### 阶段 2（第 3-6 周）：ECC Hooks
- 加 PreCommit Hook（lint + test 自动跑）
- 加 PostEdit Hook（安全扫描）
- 让团队感受到"质量自动保障了"

**门槛**：低（不改变工作流，只是自动化）
**价值**：明显（避免事故）
**接受度**：80%

#### 阶段 3（第 7-12 周）：OpenSpec
- 在新功能开发中要求用 propose
- 不强求 bug 修复也走流程
- 让团队感受到"协作变清晰了"

**门槛**：中（改变工作流）
**价值**：长期可见（变更可审计）
**接受度**：60%

#### 阶段 4（第 13+ 周）：GSD（视项目规模）
- 大型功能用 GSD 管理
- 体验上下文管理和并行执行的价值

**门槛**：高（学习新概念）
**价值**：在合适场景下显著
**接受度**：40-60%

---

### 策略 5：把规则编码进基础设施



口头约定不可靠，**必须把规则编码到不可绕过的地方**：

| 规则 | 编码位置 |
|------|---------|
| "提交前必须 lint" | Git PreCommit Hook |
| "PR 必须有 OpenSpec proposal" | GitHub Actions / GitLab CI |
| "测试覆盖率必须 > 80%" | CI 失败时阻止合并 |
| "敏感信息不能提交" | PreCommit Hook |
| "新文件必须有作者注释" | Lint 规则 |

**这是 Harness 推行成功的最大秘诀**——不依赖纪律，依赖系统。

---

## 第三部分：常见问题诊断

### 问题 1：AI 不读 CLAUDE.md

**症状**：写了 CLAUDE.md，但 AI 还是用 any 类型。

**可能原因**：
1. 文件位置不对（应在项目根目录）
2. AI 工具不会自动加载（需手动引用）
3. 内容太长，AI 注意力被稀释

**解决**：
```bash
# 1. 确认位置
ls /your-project/CLAUDE.md

# 2. 在 prompt 中明确引用
"请先阅读 CLAUDE.md，严格按照其中的规范进行开发"

# 3. 拆分大文件
# CLAUDE.md（核心约束 < 100 行）
# .claude/rules/typescript.md（详细规范）
# .claude/rules/api-design.md（详细规范）
```

---

### 问题 2：OpenSpec 的 proposal 质量差

**症状**：`/opsx:propose` 生成的 proposal 模糊、缺少细节。

**可能原因**：
1. 你给的描述太简略
2. AI 没有读取现有代码
3. 项目缺少 specs/ 主规范

**解决**：

**改进 prompt**：
```bash
# 不好的 prompt
/opsx:propose 加个收藏功能

# 好的 prompt
/opsx:propose 添加商品收藏功能。
背景：当前用户留存率 30%，想通过收藏提升回访。
范围：商品详情页可收藏，个人中心查看列表。
约束：必须支持移动端，性能要求收藏列表 < 500ms 加载。
不包含：暂不做收藏分类、不做收藏分享。
```

**让 AI 先探索**：
```bash
/opsx:explore  # 先让 AI 探索现有代码
/opsx:propose  # 再生成 proposal
```

---

### 问题 3：GSD 子代理不并行

**症状**：明明任务无依赖，但 GSD 还是串行执行。

**可能原因**：
1. AI 工具不支持子代理
2. plan.xml 没有正确标注依赖
3. 资源限制（API 限流等）

**解决**：

**检查工具支持**：
- Claude Code：原生支持子代理 ✅
- Cursor：支持 Background Agents ✅
- GitHub Copilot：暂不支持 ❌

**检查 plan.xml**：
```xml
<!-- 没有 depends_on 的任务才能并行 -->
<task id="t1">  <!-- 可以与其他无依赖任务并行 -->
<task id="t2" depends_on="t1">  <!-- 必须等 t1 完成 -->
```

---

### 问题 4：CI 中 Hooks 不生效

**症状**：本地 PreCommit Hook 工作，但在 CI 中没拦截问题。

**根因**：Git Hooks 是本地的，不会进入 CI。

**解决**：把规则同时放到 CI 配置：

```yaml
# .github/workflows/ci.yml
name: CI
on: [pull_request]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - name: Check secrets
        run: |
          if git diff origin/main..HEAD | grep -E "(api_key|password|secret).*=.*['\"]"; then
            echo "Possible secret detected!"
            exit 1
          fi
      - name: Check OpenSpec proposal
        run: |
          # 如果有代码变更，必须有对应的 OpenSpec change
          ./scripts/check-openspec-coverage.sh
```

---

### 问题 5：团队成员各用各的

**症状**：团队同时用 OpenSpec、Spec-Kit、纯 vibe coding。

**根因**：缺少强制规则。

**解决**：

**Step 1**：开会达成共识，明确**唯一选择**

**Step 2**：把选择写入团队规范：
```markdown
# CONTRIBUTING.md

## 开发流程
所有变更必须遵循以下流程：
1. 创建 OpenSpec 变更（`/opsx:propose`）
2. PR 中必须包含 OpenSpec change 文件
3. 代码 review 时同时 review change 文件
4. 合并时执行 `/opsx:archive`

不遵循此流程的 PR 将被拒绝。
```

**Step 3**：CI 中检查：
```bash
# .github/workflows/check-openspec.yml
# 如果 PR 修改了代码但没有对应的 openspec/changes/，则 fail
```

---

### 问题 6：规范和代码脱节

**症状**：openspec/specs/ 描述的是 6 个月前的状态。

**根因**：没有在 archive 时严格更新主规范。

**解决**：

**自动化检查**：
```bash
# 定期跑（每月一次）
/opsx:audit
```

这个命令（如果框架支持）会：
- 对比 specs/ 和实际代码
- 指出哪些规范已过时
- 生成"修订规范"的变更建议

**人工策略**：
- 把"更新主规范"作为 archive 的强制步骤
- Code Review 时检查 spec 是否同步更新

---

### 问题 7：上下文还是经常爆炸

**症状**：用了 GSD，但执行到一半 AI 还是糊涂。

**根因**：单个任务太大。

**解决**：

**检查任务粒度**：
```xml
<!-- 不好：一个任务做了太多 -->
<task id="t1">
  <description>实现用户认证模块（注册、登录、密码重置、双因素）</description>
</task>

<!-- 好：拆成原子任务 -->
<task id="t1"><description>实现注册 API</description></task>
<task id="t2"><description>实现登录 API</description></task>
<task id="t3"><description>实现密码重置 API</description></task>
<task id="t4"><description>实现双因素认证</description></task>
```

**应用 30 分钟原则**：每个任务必须能在 30 分钟内完成。

---

## 第四部分：度量与持续改进

### 关键度量指标



如何知道 Harness 真的有价值？测量这些指标：

#### 指标 1：返工率

**定义**：因 AI 输出质量问题导致的代码返工

**测量**：
```bash
# 每周统计
git log --since="1 week ago" --oneline | grep -E "(fix|revert|undo)" | wc -l
```

**期望趋势**：引入 Harness 后**下降 30%+**

#### 指标 2：上线事故数

**定义**：上线后发现的需要紧急修复的问题

**期望趋势**：引入 Harness 后**下降 50%+**

#### 指标 3：开发周期

**定义**：从需求开始到合并 PR 的时间

**期望趋势**：
- 短期（前 1 个月）：可能**上升 20%**（学习成本）
- 中期（2-3 个月）：回到原水平
- 长期（3 个月后）：**下降 20-30%**（自动化收益）

#### 指标 4：知识沉淀量

**定义**：CLAUDE.md / specs/ / changes/ 的总行数

**期望趋势**：稳定增长，每月 +10-20%

#### 指标 5：团队满意度

**定义**：每月一次匿名调查，问"你觉得 Harness 让工作更好了吗？"

**期望趋势**：
- 第 1 个月：可能负面（学习痛苦）
- 第 3 个月：转为中性
- 第 6 个月：明显正面

---

### 持续改进循环



每 2 周做一次回顾，按这个模板：

```markdown
# Harness 回顾 - 2026-XX-XX

## 数据
- 本期变更数：X
- 走 OpenSpec 流程的变更：X
- 因 Harness 避免的问题：X
- 因 Harness 增加的额外工作量：X

## 团队反馈
- 最受欢迎的工具：[]
- 最有争议的工具：[]
- 团队提出的改进建议：[]

## 决定
- 保留：[]
- 调整：[]
- 移除：[]
- 新增：[]

## 下期目标
- []
```

**关键**：**Harness 不是一成不变的，必须随团队和项目演进**。

---

## 第五部分：未来展望

### Harness Engineering 的演进方向

#### 方向 1：原生集成

未来 AI 编辑器会**原生**集成 Harness 概念：
- IDE 自动维护 CLAUDE.md
- 提交时自动 propose / apply / archive
- 无需手动安装框架，开箱即用

**信号**：Cursor、Claude Code 已经开始内置类似机制。

#### 方向 2：跨工具标准

类似 OpenAPI 之于 API，Harness 会有**标准化协议**：
- 统一的规范格式
- 统一的任务定义
- 跨 AI 工具的兼容性

**信号**：MCP（Model Context Protocol）的出现已是这个方向的雏形。

#### 方向 3：AI 自主管理

未来 AI 不仅是 Harness 的使用者，还是**维护者**：
- AI 自动建议更新 CLAUDE.md
- AI 检测规范漂移并提示
- AI 优化任务拆解策略

**信号**：ECC 的 `/evolve` 命令已经在做这件事。

#### 方向 4：组织级 Harness

Harness 从项目级扩展到**组织级**：
- 公司级宪法（适用所有项目）
- 跨项目的能力库共享
- 标准化的开发流程

---

### 你应该如何持续学习？

#### 短期（接下来 1-3 个月）
1. **真正落地一次** —— 选一个小项目走完整流程
2. **记录你的踩坑** —— 形成自己的最佳实践
3. **关注新框架** —— Harness 领域迭代极快，每月都有新东西

#### 中期（接下来 6-12 个月）
1. **深入各类工具协议** —— 这是 Harness 的下一个跳板
2. **学习子代理设计** —— 自己写 Agent 而非只用现成的
3. **参与社区** —— GitHub、Discord、Twitter 上有活跃讨论

#### 长期（1 年+）
1. **形成自己的方法论** —— 别人的框架是工具，自己的方法论才是能力
2. **做内部分享** —— 教是最好的学
3. **写自己的扩展** —— 基于 ECC、OpenSpec 等做团队定制

---

## 关键启发：从落地到精通的认知跃迁

很多人学完 Harness 后，会停留在"会用"层面。从"会用"到"精通"需要三个认知跃迁：

### 跃迁 1：从"框架"到"原理"

**会用**：知道 OpenSpec 怎么用
**精通**：理解为什么 OpenSpec 这么设计，看到新框架能立刻分析其设计哲学

**关键**：吃透三大核心原理（决策外化、流程阶段化、任务原子化）

### 跃迁 2：从"工具"到"系统"

**会用**：把 Harness 当成工具来使用
**精通**：把 Harness 视为一个**人机协作系统**——人的角色、AI 的角色、文件的角色、流程的角色，构成一个有机整体

**关键**：理解 Harness 不是替代人的判断，而是放大人的判断

### 跃迁 3：从"实施者"到"设计者"

**会用**：按现有框架实施
**精通**：能为自己的团队设计定制化 Harness

**关键**：识别团队特殊需求，组合或扩展现有框架

---

## 结语

Harness Engineering 不是银弹，但它是 AI 编程时代诸多**基础设施**的一种选择。

正如：

- 没有 Git，无法有现代软件协作
- 没有 CI/CD，无法有持续交付
- 没有 Docker，无法有现代部署

未来：

- **没有 Harness，无法有可靠的 AI 编程**

你今天投入学习和实践 Harness，是在为接下来的工作方式打基础。

**祝你落地顺利。**

---

## 扩展阅读

- [GitHub - spec-kit](https://github.com/github/spec-kit)
- [GitHub - OpenSpec](https://github.com/Fission-AI/OpenSpec)
- [GSD 官方文档](https://github.com/gsd-build/get-shit-done)
- [Anthropic - Claude Code 文档](https://docs.claude.com/en/docs/claude-code)
- [MCP - Model Context Protocol](https://modelcontextprotocol.io)
- [掘金 - 6 个代表性 AI 编程 Harness 工程化框架拆解](https://article.juejin.cn/post/7626998085928189998)
- [Anthropic - Building Effective AI Agents](https://www.anthropic.com/research/building-effective-agents)

---
