# AI 与计算机系统

## 引子：一个比喻的延伸

在之前的学习中，我们提到一个比喻： **"LLM 是无状态的 CPU"**。这个比喻精准地揭示了 LLM **无状态**的核心特性，它给LLM带来了：

- **灵活性与通用性**：任何 agent、任何任务都能跑
- **极致性能**：不维护状态意味着没有状态切换和维护的开销
- **可并行**：多核 CPU / 多代理同时工作
- **极高的容错与隔离**：确保了“一处崩了换另一处，重启即可恢复”的容错能力

这个比喻可以**继续延伸**——

如果 LLM 是 CPU，那么 Harness 是什么？答案是 **主板和数据总线**——是把 CPU 的纯计算能力**接入有状态的世界**（存储、外设、协议）的关键基础设施。

整个 AI 系统其实可以映射到一台**完整的计算机**，每个部件都有清晰的对应物。本文档把这个完整映射展开。读完后你会得到一个**工程师本能就能理解**的 Harness 模型——所有设计选择都能用"计算机系统怎么解的"反推。

---

## 一、为什么"无状态 + 外部状态"是计算系统的终极架构

在深入映射之前，先理解一个根本规律：

> **计算单元越无状态，系统越可扩展。**

CPU 50 年来始终保持**核心是无状态的**——这不是历史偶然，而是**四个底层原因共同决定的必然**。

#### 原因 1：极致的速度与性能

CPU 内部的电路极其昂贵和复杂。如果让 CPU 自行追踪每个任务的状态（所属用户、历史操作、调用链…），需要集成**海量的专有逻辑电路**——这需要付出巨大代价。只有把状态外包给 RAM 和硬盘，CPU 才能把所有晶体管预算花在"算得快"这一件事上。

#### 原因 2：支持多任务与上下文切换

通过保持无状态，操作系统能够**随时中断**当前任务——把 CPU 的少量核心寄存器（程序计数器、通用寄存器…）保存到内存或进程控制块（PCB）中，然后切到另一个任务。

这使得 CPU 可以**被各个任务公平、无缝地共享复用**。如果 CPU 内部保存了大量状态，"切换"就会变成"代价高昂的搬家"——多任务系统会因为状态切换的开销而无法实现。

#### 原因 3：灵活性与通用性

无状态特性赋予了 CPU **执行任意代码的能力**。它只负责单纯的"取指、解码、执行"——无论是运行游戏、浏览器还是数据库，**底层指令逻辑完全一致，不受上层软件任务性质的限制**。

#### 原因 4：极高的容错与隔离

如果 CPU 内部保存了状态，一旦发生异常或进程崩溃，**清除这些状态将变得异常困难**——崩溃可能让状态留在不一致的中间态，污染下一个任务。

无状态的特性确保了**"一处崩了换另一处，重启即可恢复"**的系统稳定性：每个任务用完 CPU 就交还，CPU 像新的一样准备接受下一个任务。

---

### 所以LLM 大概率也将会保持是无状态的

把上面四个原因平行映射到 LLM——你会发现 **LLM 必须无状态的理由与 CPU 完全同构**：

#### 理由 1：极致的速度与性能

LLM 推理本身就是高度计算密集的——每生成一个 token 需要跑过几十亿到几千亿参数的前向传播。

如果让 LLM 内部"记住"每个用户的对话历史、项目状态、领域知识，会带来三重代价：
- **推理变慢**：每次都要在巨大的"记忆"中检索
- **成本暴涨**：维护状态需要持续占用 GPU 显存（GPU 显存比 RAM 贵几十倍）
- **延迟上升**：状态越大，加载和切换越慢

把状态外包给文件系统和上下文窗口，LLM 才能把所有计算预算花在"想得对"这一件事上。

#### 理由 2：支持多用户与会话切换

通过保持无状态，AI 服务能够**随时切换服务对象**——把当前会话的少量"寄存器"（上下文窗口）保存到外部，然后切到另一个用户。

这使得**一个 LLM 实例能为成千上万用户同时服务**——你和我现在用的可能是同一颗 GPU 上的同一个模型权重，但因为它是无状态的，我们的对话互不干扰。

如果 LLM 是有状态的，"用户切换"就会变成"重新加载海量记忆"——多租户、高并发的 AI 服务在经济上根本不可行。

#### 理由 3：灵活性与通用性

无状态特性赋予了 LLM **执行任意任务的能力**。它只负责单纯的"读上下文、推理、生成"——无论是写代码、做翻译、解数学、医疗诊断，**底层推理逻辑完全一致，不受上层任务性质的限制**。

如果 LLM 为某类任务集成了专门状态（比如"代码生成专用记忆"），它执行其他任务时就会带着无用负担，甚至产生偏见。无状态保证了 LLM 的**完全通用**——同一个 Claude Sonnet 既能写代码也能写诗。

#### 理由 4：极高的容错与隔离

如果 LLM 内部保存了用户状态，一旦推理出错或服务异常，**清除这些状态将变得异常困难**——可能污染其他用户的会话，造成隐私和质量的双重灾难。

无状态特性确保了**"会话崩了重开一个、模型挂了换一台"**的系统稳定性：每次调用结束，LLM 像新的一样准备接受下一个调用。

**这也是为什么"AI Agent 出 bug 后重启会话经常就好了"——它本质上是无状态系统的天然容错机制**。

---

### 共同的演进规律

CPU 和 LLM 都遵循同一条演进规律：

> **核心计算单元越无状态，外围基础设施越发达。**

CPU 50 年的历史就是这条规律的实证：
- CPU 越保持"纯计算单元"的本色 → 外围发明出 RAM、Cache、虚拟内存、DMA、总线、协议栈、OS、文件系统…
- **所有性能突破都发生在 CPU 周围**，而不是让 CPU 本身变得"有状态"

LLM 的演进路径完全相同：
- LLM 越保持"纯推理单元"的本色 → 外围会发明出上下文管理、规范文件、Sub-Agent、MCP 协议、工作流引擎…
- **所有 AI 系统的进步都会发生在 LLM 周围**，而不是让 LLM 本身变得"有状态"

这条规律最终会走向一个推论：**Harness 的价值会长期存在**
> 它解决的是"无状态计算单元如何接入有状态世界"这个根本问题——只要 LLM 还是无状态的（而它必须无状态，理由如上），这个问题就永远存在。**学 Harness 不是学某个框架，是在学习一个永恒的计算系统范式**。

---

## 二、AI与计算机系统组件间的映射

下表把计算机系统的每个核心组件映射到  AI 系统的对应物：

| 计算机组件 | AI 系统对应物 | 角色 | 共同特征 |
|----------|---------------|------|---------|
| **CPU** | **LLM** | 计算单元 | 无状态、纯函数式、按指令执行 |
| **寄存器** | **上下文窗口** | 工作内存 | 容量小但访问极快、CPU 直接操作 |
| **L1/L2/L3 缓存** | **会话历史** | 短期缓存 | 比寄存器大、比内存快、自动淘汰 |
| **硬盘** | **文件系统（specs/、changes/）** | 持久存储 | 容量大、断电不丢、需要显式加载 |
| **主板** | **Harness 框架本身** | 系统骨架 | 连接各组件、定义协议 |
| **数据总线** | **流程阶段间的产物传递** | 信息通道 | propose → apply → archive 的产物流 |
| **中断系统** | **Hooks 事件机制** | 异步响应 | PreCommit、PostEdit 等触发器 |
| **多核 CPU** | **波次并行的 Sub-Agents** | 并行计算 | 同时执行无依赖任务 |
| **I/O 端口** | **MCP 协议** | 外设接口 | 标准化的外部资源访问 |

---

## 三、三个层次的精妙对应

更深入地看，这种类比在三个层次上都成立：

### 层次 1：硬件层（Hardware）

```
计算机：CPU + 内存 + 硬盘 + 总线 + I/O
         ↓
AI 系统：LLM + 上下文 + 文件系统 + 流程协议 + MCP
```

**共同抽象**：**计算 + 存储 + 通信**——任何"做事的系统"都需要这三要素。

### 层次 2：操作系统层（OS）

```
计算机 OS：进程调度、内存管理、文件系统、中断处理、设备驱动
         ↓
Harness OS：Agent 调度、上下文管理、规范文件管理、Hook 触发、MCP 集成
```

**共同抽象**：**资源管理与协调**——OS 的本质就是"把硬件能力组织起来服务上层应用"。

### 层次 3：应用层（Application）

```
计算机应用：用户操作 → 应用程序 → 系统调用 → OS → 硬件
         ↓
AI 应用：用户需求 → 工作流 → Agent 调用 → 主代理 → LLM
```

**共同抽象**：**意图到执行的层层翻译**。

---

## 四、几个特别精妙的对应（深度展开）

### 对应 1：缓存层级（Cache Hierarchy）→ 上下文层级

CPU 的缓存设计哲学：**越靠近 CPU 越快但越小**。

```
寄存器 (~1KB, 1 cycle)
  ↓
L1 Cache (~64KB, 4 cycles)
  ↓
L2 Cache (~256KB, 12 cycles)
  ↓
L3 Cache (~10MB, 40 cycles)
  ↓
RAM (~16GB, 200 cycles)
  ↓
SSD (~1TB, 100,000 cycles)
```

Harness 的上下文设计哲学**完全同构**：

```
当前 prompt（最贵、最直接，几百 tokens）
  ↓
最近几轮对话（自动保留，几 K tokens）
  ↓
当前激活的文件（显式 Read 加载，几十 K tokens）
  ↓
项目所有文件（按需加载，几 MB-GB）
  ↓
外部知识（MCP/搜索，理论无限）
```

**这就是为什么"按需加载"是 Harness 的核心策略**——它在复刻 CPU 缓存设计 50 年验证过的智慧。

### 对应 2：Cache Miss → Context Miss

CPU 设计中最大的性能损失是 **Cache Miss**——CPU 要的数据不在 cache 里，要去更慢的层级取。

Harness 中对应的是 **Context Miss**——AI 需要的信息不在当前上下文中：
- AI 不知道"项目用什么命名规范"→ 必须 Read CLAUDE.md（Page Fault）
- AI 不知道"上一个变更改了什么"→ 必须 Read changes/（Page Fault）

**性能优化的目标完全一致**：最大化 cache hit rate（让需要的信息已经在快速层级）。

| 优化策略 | CPU 系统 | Harness 系统 |
|---------|---------|-------------|
| **预取（Prefetch）** | 预测下一条要执行的指令 | SessionStart Hook 预加载常用文件 |
| **空间局部性** | 顺序内存访问 | 把相关文件放到同一目录 |
| **时间局部性** | 最近访问的最可能再访问 | 当前任务相关文件保持在上下文中 |

### 对应 3：硬件中断 → Hooks

CPU 大部分时间在执行指令，但需要**响应外部事件**：键盘按键、网络数据包、定时器到期。

CPU 用**中断系统**实现：当事件发生，CPU 暂停当前任务，跳转到中断处理程序。

Harness 中的 **Hooks** 完全同构：

| 中断源 | 中断处理 |
|-------|---------|
| 用户编辑文件 | PostEdit Hook → 触发安全扫描 |
| 用户提交代码 | PreCommit Hook → 触发测试 |
| 用户开始会话 | SessionStart Hook → 加载上下文 |
| Build 失败 | OnError Hook → 调用 build-error-resolver |

**两者都解决了"事件驱动 vs 同步处理"的矛盾**——让被动响应也能高效。

### 对应 4：MMU + 虚拟内存 → 上下文虚拟化

CPU 的 MMU（内存管理单元）创造了一个伟大的抽象：**虚拟内存**。
程序以为自己有无限内存，实际上 OS 只把当前需要的页面加载到物理内存，其他存在硬盘。

Harness 也在做同样的事：
- AI 以为自己"知道整个项目"
- 实际上每次只加载当前任务相关的几个文件
- 其他文件躺在文件系统里，需要时再"换入"

**这是为什么"决策外化"能突破上下文窗口的物理限制**——它在创造"虚拟上下文"。

类比关系：

| 计算机 | Harness |
|-------|---------|
| 物理内存（4GB） | 上下文窗口（200K tokens） |
| 虚拟地址空间（48-bit, 256TB） | 整个文件系统（无上限） |
| MMU 翻译虚拟地址→物理地址 | AI 通过 Read 工具按路径加载文件 |
| Page Fault：访问未加载的页 | Context Miss：需要未加载的文件 |
| Page Replacement Algorithm（LRU 等） | 上下文管理策略（按需加载/清理） |

###  对应 5：协处理器（GPU）→ 专业 Sub-Agent

CPU 是通用的，但有些任务（图形渲染、矩阵运算）用专用硬件更快。
GPU、DSP、TPU 等**协处理器**应运而生——CPU 把特定任务委托给它们。

Harness 中的对应：通用 LLM 能做所有事，但特定任务有专用 Agent 更好：

| 通用计算 | 专用协处理器 |
|---------|------------|
| 主代理 LLM | security-reviewer Agent（红蓝队对抗专精） |
| 主代理 LLM | tdd-tester Agent（TDD 流程专精） |
| 主代理 LLM | build-error-resolver Agent（错误模式专精） |

**通用计算单元 + 专用协处理器**是计算系统的永恒模式，AI 系统正在重演。

---

## 五、完整意象图

把所有部分组装起来：

```
┌──────────────────────────────────────────────────────────────────┐
│                    Harness Engineering 完整架构                    │
│                  （类比计算机系统的完整意象）                        │
└──────────────────────────────────────────────────────────────────┘

         ┌─────────────────────────────┐
         │   用户层（Application）      │
         │   开发者 / 团队 / 业务需求    │
         └──────────────┬──────────────┘
                        ↓ 系统调用
         ┌─────────────────────────────────────────────────┐
         │   操作系统层（Harness Kernel）                    │
         │   主代理（Orchestrator）+ 工作流引擎              │
         │   - Agent 调度                                  │
         │   - 上下文管理                                   │
         │   - 流程编排                                     │
         └─────┬───────────────┬───────────────┬───────────┘
               ↓               ↓               ↓
        ┌──────────┐    ┌──────────┐    ┌──────────┐
        │  CPU     │    │  存储     │    │  外设     │
        │  LLM     │    │  文件系统 │    │  MCP     │
        │          │    │           │    │           │
        │ 寄存器：  │    │ specs/    │    │ GitHub   │
        │ 上下文窗口│    │ changes/  │    │ DB       │
        │          │    │ CLAUDE.md │    │ Cloud    │
        │ Sub-Agent│    │           │    │ Slack    │
        │ (多核)   │    │           │    │ ...       │
        └────┬─────┘    └────┬─────┘    └────┬─────┘
             │                │                │
             └────────────────┴────────────────┘
                              ↑
                ┌─────────────────────────────┐
                │   总线 + 协议层               │
                │   - 流程阶段（时钟）          │
                │   - 文件读写（DMA）           │
                │   - Hooks（中断）            │
                │   - MCP（I/O 协议）          │
                └─────────────────────────────┘
```

---

## 六、这个完整意象的设计启示

### 启示 1：复用计算机系统 50 年的设计智慧

计算机体系结构经过 50 年演进沉淀的解决方案，AI 系统不必重新发明：

| 计算机已解决的问题 | Harness 的对应方案 |
|-------------------|-------------------|
| CPU 太快，内存太慢 → 多级缓存 | LLM 太通用，文件太多 → 上下文层级加载 |
| CPU 内存有限，但程序需要大空间 → 虚拟内存 | 上下文窗口有限，但项目大 → 按需加载文件 |
| CPU 不能等所有事件 → 中断系统 | AI 不能持续监控 → Hooks 机制 |
| 通用 CPU 不擅长所有任务 → 协处理器 | 通用 LLM 不专精 → 专业 Sub-Agent |
| 所有 I/O 经 CPU 慢 → DMA | AI 经用户慢 → 直接文件读写 |
| 设备厂商互不兼容 → 标准协议（USB/PCIe） | 工具厂商互不兼容 → MCP 协议 |

**当你设计自己的 Harness 时，问"计算机系统遇到这个问题时怎么解的"——往往直接有答案**。

### 启示 2:理解为什么 MCP 等工具协议如此重要

计算机系统的标准化是分层的：
- ISA（指令集）标准化 → CPU 厂商可以独立创新
- USB/PCIe 标准化 → 外设厂商可以独立创新
- POSIX 标准化 → 应用可以跨 OS

AI 系统正在经历同样的标准化过程：
- LLM API 协议 → 模型厂商可以独立创新
- **MCP 协议 → 工具/数据源厂商可以独立创新**
- Sub-Agent 协议（演进中）→ 编排框架可以独立创新

**MCP 之于 Harness，就像 USB 之于 PC**——它定义了"AI 系统的外设标准"。

### 启示 3：理解为什么"原子化 + 并行"是性能关键

CPU 的性能突破不是来自单核越来越快（频率早就触顶），而是来自：
- **多核并行**
- **流水线（pipeline）**
- **乱序执行**

Harness 的性能突破也会沿着同样的路径：
- **多 Sub-Agent 并行**（已经实现：GSD 的波次执行）
- **任务流水线**（GSD 在尝试）
- **乱序执行**（前沿研究：根据依赖图自动调度）

**单个 LLM 越快不重要——重要的是如何并行调度多个 LLM**。

### 启示 4：长期价值判断

这个映射给出了一个**长期价值判断框架**：

| 哪些会变？ | 哪些不变？ |
|-----------|-----------|
| LLM 的具体能力 | LLM 是无状态计算单元的本质 |
| 具体框架（Spec-Kit/OpenSpec/...） | 决策外化、流程结构化、任务原子化的必要性 |
| MCP 协议的具体形式 | "需要标准化外设接口"的需求 |
| 上下文窗口的大小 | "需要分层加载"的设计思路 |

**学 Harness 的本质**：学的不是某个具体框架，而是**计算机系统这个"已经成熟 50 年的设计模式库"在 AI 时代的复刻**。

理解了这一点，你就能在任何新工具出现时，快速定位它在大图景中的位置——它对应计算机系统的哪个组件？解决了哪个老问题？

---

## 七、检验你对这个心智模型的掌握

读完这篇，应该能回答以下问题：

- [ ] 为什么"无状态 + 外部状态"是计算系统的终极架构？
- [ ] 为什么 CPU 缓存设计能直接启发 Harness 的上下文管理？
- [ ] 为什么 Hooks 本质上是"中断系统"？
- [ ] 为什么 MCP 协议在 AI 系统中的地位类似 USB 在 PC 中的地位？
- [ ] 当你看到一个新 Harness 框架时，能用计算机系统中的哪个组件类比它？

如果都能答上来，你就拥有了一个**工程师本能就能理解**的 Harness 心智模型。

---

## 八、关键启发

这篇文档最深的启发不是某个具体类比，而是一个**思考方式**：

> **AI 系统不是"全新的领域"——它是计算机系统设计模式在 AI 时代的复刻。**

这意味着：

1. **设计 Harness 不必从零开始**——计算机系统 50 年的智慧都可以借鉴
2. **判断框架价值不必猜测未来**——看它对应计算机系统的哪个组件，参考那个组件的演进史
3. **学习成本可以大幅降低**——你已经知道计算机怎么工作，现在只需把这套知识"翻译"到 AI 领域

**最优秀的工程师不是学得最多的人，而是看得最远的人——能在新领域中识别老模式的人。**

Harness Engineering 这个新领域，骨子里仍然是经典计算机系统的范式。**理解这一点，你就站在了正确的高度上。**

---

## 扩展阅读

- [Computer Architecture: A Quantitative Approach（计算机体系结构经典教材）](https://www.elsevier.com/books/computer-architecture/hennessy/978-0-12-811905-1)
- [Operating Systems: Three Easy Pieces（OS 经典开源教材）](https://pages.cs.wisc.edu/~remzi/OSTEP/)
- [MCP - Model Context Protocol](https://modelcontextprotocol.io)
- [Anthropic - Claude Code 文档](https://docs.claude.com/en/docs/claude-code)

---