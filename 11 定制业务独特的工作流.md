# 定制业务独特的工作流

## 引子：从"用别人的"到"造自己的"

学完前面所有文档，你应该已经发现一个事实：

> **没有任何一个现成的 Harness 框架是为你的项目量身打造的。**

OpenSpec 设计时考虑的是"通用棕地项目"，ECC 设计时考虑的是"Claude Code 重度用户"，GSD 设计时考虑的是"长周期复杂项目"——它们的设计哲学都是基于**特定假设**的。当你的项目不符合这些假设时，强行套用就会出现摩擦。

真正的工程能力不是"会用这些工具"，而是：

1. **看穿表面**：理解每个工具背后的设计原理（已完成）
2. **识别局限**：清晰知道每个工具不擅长什么（本文重点）
3. **评估现状**：诚实评估自己的项目和环境（本文重点）
4. **做出取舍**：在设计空间中做出明智选择（本文重点）
5. **持续演进**：随项目演进调整工作流

这就是 Harness Engineering 的第一性原理：**Harness 不是配置出来的，是设计出来的**。

---

## 第一部分：理解现有实践的局限（前置阅读）

**定制工作流的起点，是清晰认识现有框架"为什么不够用"。**

这部分内容已经在 **[快速试用与适配性评估](https://iwiki.woa.com/p/4021217224) ** 的"试用前必读：现有框架的局限性"章节做过完整剖析，包括：

- **OpenSpec 的 5 项局限**：Delta 认知负担 / 主规范熵增 / 跨仓库薄弱 / 执行隔离弱 / 审计粒度
- **ECC 的 5 项局限**：能力发现成本 / 质量天花板 / Hooks 隐式复杂度 / 生态绑定 / 缺乏流程指引
- **GSD 的 5 项局限**：三层分解过度 / XML 摩擦 / Sub-Agent 硬依赖 / 不管规范 / 流程僵硬
- **Spec-Kit 的 4 项局限**：宪法冷启动悖论 / 全量规范维护压力 / Brownfield 不友好 / 阶段切换僵硬
- **共性局限**：AI 能力基线假设 / 工程文化稳定假设 / 缺乏轻重切换 / 决策外化层次不全

> 📖 **强烈建议**：在继续阅读后续部分之前，先温习这些局限。
> 定制工作流的本质就是**针对性地补足这些局限**——没有清晰的局限认知，定制就没有靶点。

如果你已经做过试用，这些局限对你而言不再只是文字——而是你**亲自踩过的坑**。带着这种切身体会进入下一部分，设计决策会更有针对性。

---

## 第二部分：评估你的现状的关键维度

**在做任何技术选择之前，先把现状评估清楚。**

以下是设计 Harness 时必须诚实评估的 6 大维度。每个维度都直接影响后续的设计决策。

---

### 维度 1：代码库特征（Codebase）

#### 1.1 项目阶段

| 阶段 | 特征 | 对 Harness 设计的影响 |
|------|------|---------------------|
| **探索期** | 需求不稳定、架构频繁变 | 流程要轻、规范要可丢弃 |
| **建设期** | 需求逐渐清晰、架构固化中 | 流程开始加重、规范开始固化 |
| **成熟期** | 需求稳定、架构稳定 | 流程严格、规范权威 |
| **维护期** | 主要修 bug 和小优化 | 流程要快速、规范用于约束 |
| **重构期** | 主动改造架构 | 流程要支持破坏性变更 |

**关键问题**：
- 你的项目当前在哪个阶段？
- 这个阶段的核心矛盾是什么？

#### 1.2 仓库结构

| 结构 | 设计影响 |
|------|---------|
| **单仓库 + 单服务** | 最简单，框架原生支持 |
| **单仓库 + Monorepo** | 需要规范是否按模块/包分割 |
| **多仓库 + 紧耦合** | 需要中央协调机制（共享 spec/中央 change） |
| **多仓库 + 松耦合** | 各仓库独立 Harness，靠契约对齐 |

**关键问题**：
- 跨仓库变更的频率？
- 有没有"契约"概念（API、消息格式、共享库）？
- 谁负责跨仓库协调？

#### 1.3 代码质量基线

| 基线 | 设计影响 |
|------|---------|
| **高质量** | Harness 可专注于"快速变更" |
| **中质量** | Harness 需要包含质量门禁 |
| **低质量** | Harness 必须强制最低质量标准 |
| **遗留代码** | Harness 需要"豁免区"机制 |

**关键问题**：
- 测试覆盖率？
- 文档覆盖率？
- 类型化程度？
- 重复代码情况？

---

### 维度 2：变更模式（Change Patterns）

#### 2.1 变更频率与规模分布

| 评估你过去 3 个月的变更分布 | 设计影响 |
|---------|---------|
| **小变更主导（C 列）** | 流程要极轻，重流程对小变更是负担 |
| **均衡分布（B 列）** | 需要"轻重切换"机制 |
| **大变更主导（A 列）** | 流程可以重，每次变更值得投入 |

#### 2.2 变更类型

| 类型 | 占比影响 |
|------|---------|
| **新功能开发** | 占比高 → 重视规范驱动（OpenSpec/Spec-Kit） |
| **Bug 修复** | 占比高 → 重视快速诊断（ECC 的 build-error-resolver） |
| **性能优化** | 占比高 → 重视测量与验证 |
| **重构** | 占比高 → 重视破坏性变更管理（OpenSpec 的 REMOVED） |
| **配置调整** | 占比高 → 可能不需要复杂 Harness |

#### 2.3 变更紧急度

| 紧急度 | 设计影响 |
|--------|---------|
| **可计划（80%+）** | 流程可以重，按部就班 |
| **半紧急（出现频繁）** | 必须有"快速通道" |
| **频繁紧急（运维项目）** | 流程必须极轻 |

---

### 维度 3：团队特征（Team）

> 注：我们只看影响**技术决策**的团队特征，不涉及推行策略。

#### 3.1 团队规模与协作模式

| 规模 | 设计影响 |
|------|---------|
| **单人** | 不需要协作机制，但需要"未来的自己能看懂"的记录 |
| **小团队（2-5 人）** | 协作靠默契，规范文件可以少 |
| **中团队（6-20 人）** | 必须有显式规范，否则难一致 |
| **大团队（20+ 人）** | 必须分层规范（全局/模块/特性） |

#### 3.2 AI 编程经验分布

| 经验分布 | 设计影响 |
|---------|---------|
| **整团队都熟练** | 可以引入复杂框架 |
| **少数熟练，多数新手** | 框架要降低使用门槛 |
| **整团队都新手** | 从最简框架开始 |

#### 3.3 工程师角色分布

| 角色配比 | 设计影响 |
|---------|---------|
| **全栈为主** | 任务原子化粒度可以大 |
| **专业分工细** | 任务原子化粒度要小，按职责切分 |
| **有架构师** | 决策外化的"原则层"由架构师维护 |
| **没有架构师** | 决策外化的"原则层"靠集体维护 |

---

### 维度 4：AI 工具能力（Tooling）

**这是最容易被忽视但影响最大的维度。**

#### 4.1 上下文窗口大小

| 窗口大小 | 设计影响 |
|---------|---------|
| **< 100K** | 必须激进的上下文管理（强外化、强隔离） |
| **100K-200K** | 标准 Harness 设计假设 |
| **> 200K** | 可以放松上下文管理，但仍需注意 Lost in the Middle |
| **> 1M（实验性）** | Harness 价值降低？（不一定，下文详述） |

#### 4.2 子代理（Sub-Agent）支持

| 支持程度 | 设计影响 |
|---------|---------|
| **原生支持（Claude Code 等）** | 可以完整实现 GSD 风格的并行 |
| **背景代理（Cursor 等）** | 部分支持并行 |
| **多会话（任意工具）** | 可以人工模拟并行 |
| **不支持** | 必须串行设计，重视上下文清理 |

#### 4.3 文件系统访问

| 访问能力 | 设计影响 |
|---------|---------|
| **完整 R/W** | 可以完整外化决策到文件 |
| **只读** | 决策外化要靠人维护 |
| **沙箱限制** | 跨目录的 Harness 设计受限 |

#### 4.4 工具间一致性

| 你的情况 | 设计影响 |
|---------|---------|
| **团队统一一种工具** | 可以深度定制 |
| **多种工具混用** | Harness 必须工具中立 |

---

### 维度 5：质量与合规要求（Quality）

#### 5.1 业务关键性

| 关键性 | Harness 重点 |
|--------|-------------|
| **核心业务系统** | 强验证、强审计、强回滚 |
| **支撑系统** | 标准 Harness 即可 |
| **内部工具** | 可以放松流程 |
| **实验性项目** | 可以无流程 |

#### 5.2 监管与审计

| 要求 | 设计影响 |
|------|---------|
| **金融/医疗合规** | 变更全审计（OpenSpec + 详细 commit history） |
| **GDPR 等隐私法规** | 数据流变更必须留痕 |
| **内部审计** | 变更动机必须文档化 |
| **无监管** | 灵活设计 |

#### 5.3 安全敏感度

| 敏感度 | 设计影响 |
|--------|---------|
| **极高（密码学/认证）** | 必须有红蓝队审计（参考 ECC 的 AgentShield） |
| **高（用户数据）** | 安全扫描必须自动化 |
| **中** | 提交前安全检查 |
| **低** | 不需要专门的安全 Harness |

---

### 维度 6：成本约束（Cost）

#### 6.1 Token 预算

| 预算 | 设计影响 |
|------|---------|
| **充足** | 可以用多代理、详细规范 |
| **紧张** | 必须激进上下文裁剪、避免重复加载 |
| **极紧张** | 简化到极致，靠人脑补 |

#### 6.2 开发时间预算

| 预算 | 设计影响 |
|------|---------|
| **充足（长期项目）** | 投入构建完整 Harness |
| **紧张** | 选最小有效集合 |
| **极紧张** | 只用 CLAUDE.md + 自动化 Hooks |

#### 6.3 维护精力预算

| 预算 | 设计影响 |
|------|---------|
| **有专人维护** | 可以构建复杂 Harness |
| **大家兼顾** | 必须降低维护成本 |
| **几乎无人维护** | 设计自维护机制（自动化检查） |

---

## 第三部分：技术设计空间

完成现状评估后，你需要在三大原理的设计空间中做出选择。这不是"选哪个框架"，而是"在这个原理上做什么样的工程实现"。

---

### 设计空间 1：决策外化怎么做

#### 选择 1.1：外化文件的格式

| 选项 | 优势 | 劣势 | 适用 |
|------|------|------|------|
| **Markdown** | 人可读、AI 友好、git diff 友好 | 结构弱、机器解析有歧义 | 大多数场景默认选择 |
| **XML** | 强结构、可校验 | 人读写不友好、git diff 难看 | 复杂任务编排（如 GSD plan.xml） |
| **YAML** | 比 XML 简洁、有结构 | 缩进敏感、字符串规则复杂 | 配置类决策（constitution） |
| **JSON** | 通用、机器友好 | 人读写最不友好 | 程序间交互 |
| **混合** | 各取所长 | 维护多种格式 | 大型项目 |

**第一性原理决策**：
- 决策对**机器**重要（自动调度） → 强结构（XML/JSON/YAML）
- 决策对**人**重要（团队共识） → 弱结构（Markdown）
- 默认选 **Markdown**——大多数决策都是给人看的

#### 选择 1.2：外化文件的粒度

| 粒度 | 适用 |
|------|------|
| **单一巨型文件**（一个 CLAUDE.md 涵盖一切） | 单人/小团队/小项目 |
| **按层次分文件**（principles + specs + state） | 大多数场景 |
| **按模块分文件**（每个服务/模块独立目录） | 微服务/Monorepo |
| **混合（全局+局部）** | 大型项目 |

**第一性原理决策**：
- **单文件**：当所有决策都是全局约束时
- **多文件**：当决策有明显的"作用域"差异时（全局 vs 模块 vs 特性）
- 默认从**单文件**开始，演进到**多文件**

#### 选择 1.3：外化的存储位置

| 位置 | 优势 | 劣势 |
|------|------|------|
| **代码仓库根目录** | 与代码一起版本控制 | 增加仓库复杂度 |
| **代码仓库 .claude/ 目录** | 隔离工具相关文件 | AI 工具特定 |
| **独立的 spec 仓库** | 解耦、可跨项目 | 同步成本 |
| **外部知识库（Notion 等）** | 团队习惯 | 与代码脱节、不适合 AI 加载 |

**第一性原理决策**：
- AI 必须能直接读取
- 必须与代码版本同步演进
- 默认选**代码仓库内**（根目录或 `.claude/`）

#### 选择 1.4：更新模型

| 模型 | 描述 | 适用 |
|------|------|------|
| **全量快照** | 每次更新都重写完整文档 | 小文档、变化不频繁 |
| **增量 Delta** | 只描述变化（OpenSpec 模式） | 大型规范、频繁变化 |
| **混合（snapshot + delta log）** | 主文档 + 变更日志 | 高审计要求 |
| **append-only** | 永远追加不修改 | ADR 模式 |

**第一性原理决策**：
- 关心"变化历史" → Delta 或 Snapshot+Log
- 只关心"当前状态" → Snapshot
- 关心"决策动机" → append-only（每个决策独立保留）

#### 选择 1.5：决策的层次划分

你需要决定外化哪些层次的决策：

```
全局原则层
   ↓ 适用于
  系统层
   ↓ 适用于
  模块层
   ↓ 适用于
  特性层
   ↓ 适用于
  任务层
```

**第一性原理**：
- 越上层，外化优先级越高（影响范围广）
- 越下层，外化必要性递减（被代码本身表达）
- 至少必须外化**全局原则层**——这是底线

---

### 设计空间 2：流程结构化怎么做

#### 选择 2.1：阶段数量

| 阶段数 | 描述 | 适用 |
|--------|------|------|
| **0 阶段** | 无流程，直接编码 | 探索性、prototype |
| **2 阶段（设计→实现）** | 极简 | 小变更、紧急修复 |
| **3 阶段** | propose/apply/archive 或 plan/code/verify | 中等复杂度（OpenSpec 默认） |
| **5 阶段** | 完整 SDLC | 大型变更（GSD/Spec-Kit） |
| **7+ 阶段** | 过度设计 | 一般不推荐 |

**第一性原理决策**：
- 阶段数 = max(2, 你的变更涉及的"决策类型数")
- 决策类型：what/why/how/who/when/test/ship
- **多数项目 3-5 阶段足够**

#### 选择 2.2：阶段间是"门"还是"桥"

| 模型 | 描述 | 优势 | 劣势 |
|------|------|------|------|
| **硬门（Strict Gate）** | 上一阶段不完成不能进下一阶段 | 严谨 | 僵硬 |
| **软门（Soft Gate）** | 推荐顺序但允许跳跃 | 灵活 | 可能滥用 |
| **桥（Bridge）** | 阶段产物相互引用 | 自然 | 缺乏强制 |

**第一性原理决策**：
- 质量优先 → 硬门
- 速度优先 → 软门
- 默认 → 软门 + 强制产物

#### 选择 2.3：阶段产物的定义

每个阶段必须定义：
- **输入**：从哪里来
- **输出**：产生什么文件
- **验收标准**：完成的判定条件
- **回退条件**：什么情况下回退到上一阶段

**陷阱**：很多团队只定义了"做什么"，没定义"完成判定"。结果阶段化沦为形式。

**第一性原理决策**：
- 每个阶段必须有**可机器验证**的产出
- 没有产物的阶段不是阶段，是 ritual

#### 选择 2.4：是否支持"轻重切换"

**这是现有框架普遍欠缺的能力。**

设计选项：
| 选项 | 描述 |
|------|------|
| **单一流程** | 所有变更走相同流程（OpenSpec 默认） |
| **多档位流程** | 根据变更规模选择流程（推荐） |
| **动态流程** | AI 自动判断该走哪档（实验性） |

**多档位流程示例**：

```
档位 A：紧急修复（2 阶段）
  - quick-plan: 描述问题和方案（1 段话）
  - implement: 修复并提交

档位 B：日常变更（3 阶段）
  - propose: 简化版提案
  - apply: 实现
  - archive: 归档

档位 C：大型功能（5 阶段）
  - propose: 完整提案
  - design: 详细技术设计
  - apply: 多任务并行实现
  - verify: Goal-Backward 验证
  - archive: 归档+主规范更新
```

**第一性原理决策**：
- 如果你的变更分布是"长尾"——少数大变更、多数小变更——必须有多档位
- 否则要么过度设计（小变更走重流程），要么过度简化（大变更走轻流程）

---

### 设计空间 3：任务原子化怎么做

#### 选择 3.1：任务粒度

| 粒度 | 描述 |
|------|------|
| **细粒度（< 30 分钟）** | GSD 标准 |
| **中粒度（30 分钟-2 小时）** | 大多数适用 |
| **粗粒度（> 2 小时）** | 简单项目可接受 |

**第一性原理决策**：
- 粒度 ≈ 单上下文能完整加载相关文件 + 完成实现 + 验证的边界
- 不是"30 分钟"是教条，是**当前模型在你项目的实际表现决定**
- 测试方法：选 5 个最近的任务，看 AI 在 50% 上下文内能否完成

#### 选择 3.2：依赖管理方式

| 方式 | 描述 | 适用 |
|------|------|------|
| **隐式（顺序执行）** | tasks.md 列表顺序就是依赖 | 简单项目 |
| **显式 DAG** | 每个任务标注 depends_on | 复杂项目 |
| **显式 + 自动检测** | AI 分析代码识别依赖 | 高级 |

**第一性原理决策**：
- 任务间依赖少 → 隐式
- 任务间依赖多 → 显式
- 默认从隐式开始，复杂时升级

#### 选择 3.3：执行模型

| 模型 | 描述 | 前置要求 |
|------|------|---------|
| **串行（单代理）** | 一个个执行 | 任意工具 |
| **波次并行（GSD 模式）** | 无依赖的任务并行 | 子代理支持 |
| **流水线（Pipeline）** | 任务流水线化 | 高级编排 |

**第一性原理决策**：
- 工具不支持子代理 → 必须串行
- 任务并行收益 < 子代理调度开销 → 串行
- 任务并行收益 > 调度开销 → 并行

#### 选择 3.4：上下文加载策略

| 策略 | 描述 |
|------|------|
| **全量加载** | 每个任务加载所有相关文件 |
| **按需加载** | 任务定义中显式列出 context_files |
| **AI 决策** | 让 AI 自己决定加载什么 |
| **混合** | 核心文件全量+扩展文件按需 |

**第一性原理决策**：
- 上下文窗口大、任务范围窄 → 全量
- 上下文窗口紧、任务范围广 → 按需
- 任务定义清晰 → 显式 context_files
- 任务定义模糊 → 让 AI 决策（但效果不稳定）

#### 选择 3.5：验证策略

| 策略 | 描述 |
|------|------|
| **无验证** | 任务完成即为完成 |
| **单元测试验证** | 任务关联测试通过 |
| **集成测试验证** | 端到端流程通过 |
| **Goal-Backward 验证** | 用户场景可达成（GSD 模式） |
| **多层验证** | 单元+集成+Goal-Backward |

**第一性原理决策**：
- 风险高 → 多层验证
- 测试体系成熟 → 单元+集成
- 测试体系弱 → Goal-Backward（更接近真实价值）
- 实验性 → 无验证（接受失败）

---

## 第四部分：从评估到设计的决策框架

把现状评估（第二部分）和技术选择（第三部分）连接起来——这才是"定制 Harness"的核心动作。

---

### 决策框架的四个步骤

```
┌────────────────────┐
│  Step 1            │
│  现状评估           │
│  6 大维度打分       │
└────────┬───────────┘
         ↓
┌────────────────────┐
│  Step 2            │
│  识别核心矛盾       │
│  哪些维度最严苛？    │
└────────┬───────────┘
         ↓
┌────────────────────┐
│  Step 3            │
│  设计空间选择       │
│  三大原理各做什么决策 │
└────────┬───────────┘
         ↓
┌────────────────────┐
│  Step 4            │
│  原型验证           │
│  小范围试用，迭代   │
└────────────────────┘
```

---

### Step 1：现状评估打分表

按第二部分的 6 大维度，给自己的项目打分：

```markdown
# 项目 Harness 现状评估

## 1. 代码库特征
- 项目阶段：[探索/建设/成熟/维护/重构]
- 仓库结构：[单仓单服务/单仓 Monorepo/多仓紧耦合/多仓松耦合]
- 代码质量基线：[高/中/低/遗留]

## 2. 变更模式
- 变更分布：[小变更主导/均衡/大变更主导]
- 变更类型主要是：[新功能/Bug 修复/性能优化/重构/配置]
- 变更紧急度：[可计划/半紧急/频繁紧急]

## 3. 团队特征
- 团队规模：[单人/小/中/大]
- AI 经验分布：[整体熟练/少数熟练/整体新手]
- 角色分布：[全栈/分工细/有架构师/无架构师]

## 4. AI 工具能力
- 上下文窗口：[< 100K / 100-200K / > 200K]
- 子代理支持：[原生/部分/无]
- 工具一致性：[统一/混用]

## 5. 质量与合规
- 业务关键性：[核心/支撑/内部/实验]
- 监管要求：[金融医疗/GDPR/内部审计/无]
- 安全敏感度：[极高/高/中/低]

## 6. 成本约束
- Token 预算：[充足/紧张/极紧张]
- 时间预算：[充足/紧张/极紧张]
- 维护精力：[专人/兼顾/几乎无]
```

---

### Step 2：识别核心矛盾

打分后，**识别 3 个最受约束的维度**——这是你的核心矛盾。

例子：

```
核心矛盾 1：维护期 + 多仓紧耦合
  → 流程必须支持跨仓库快速变更，且不破坏稳定性

核心矛盾 2：团队整体新手 + Token 预算紧张
  → Harness 不能学习成本太高，也不能消耗太多 token

核心矛盾 3：核心业务系统 + 子代理支持有限
  → 必须有强验证机制，但不能依赖并行执行
```

**为什么这步关键？**
- 不识别核心矛盾，会陷入"想要所有好处"的陷阱
- Harness 设计是**取舍游戏**，不是堆砌游戏

---

### Step 3：设计空间选择

针对识别出的核心矛盾，在三大原理的设计空间中做选择。

**示例 1：紧急修复频繁的运维项目**

核心矛盾：变更紧急度高 + 质量要求高

设计选择：
- **决策外化**：极简（只保留 CLAUDE.md + 关键 ADR）
- **流程结构化**：双档位（紧急通道 2 阶段 / 日常 3 阶段）
- **任务原子化**：粗粒度（避免拆解开销）
- **验证策略**：自动化测试 + 自动化部署回滚

**示例 2：探索期的初创项目**

核心矛盾：需求不稳定 + 团队小 + 时间紧

设计选择：
- **决策外化**：仅 CLAUDE.md（不写规范）
- **流程结构化**：无流程或单档位（quick-plan + implement）
- **任务原子化**：跳过（直接写）
- **验证策略**：人工验收（接受快速失败）

**示例 3：合规要求极高的金融系统**

核心矛盾：业务关键 + 监管严 + 多仓微服务

设计选择：
- **决策外化**：分层（全局宪法 + 服务规范 + 变更提案 + 状态文件）
- **流程结构化**：5 阶段硬门
- **任务原子化**：细粒度 + 显式 DAG + 多层验证
- **验证策略**：Goal-Backward + 自动安全扫描 + 人工审计

---

### Step 4：原型验证

不要一次性设计完美的 Harness——**先做最小可行版本，再迭代**。

#### 最小可行 Harness（MVH）

任何 Harness 至少包含：
1. 一个 CLAUDE.md（项目元信息）
2. 一种"做大变更前先想想"的机制（不一定是 propose，可以是简单的 design.md）
3. 一个自动化检查（哪怕只是 lint）

**这就够了**——加任何东西都需要被收益证明。

#### 迭代触发条件

什么时候该升级 Harness？

| 触发条件 | 升级方向 |
|---------|---------|
| 经常出现"AI 不知道项目约定" | 加强 CLAUDE.md / 加分层规范 |
| 经常返工 | 加 propose 阶段 |
| 经常上下文爆炸 | 引入任务原子化 / 子代理 |
| 经常发现已上线问题 | 加 verify 阶段 |
| 经常找不到决策动机 | 加变更归档 |
| 团队抱怨流程繁琐 | 引入"轻重切换"机制 |

**第一性原理**：每个升级必须解决一个**已经发生**的问题，不是"未来可能有用"。

---

## 第五部分：超越现有框架的设计模式

理解了第一性原理后，你可以设计现有框架不具备的能力。这里给出几个有价值的设计模式。

---

### 设计模式 1：动态档位切换

**问题**：现有框架要么"一直重"要么"一直轻"。

**设计**：根据变更特征自动选择流程档位。

**实现示意**：

```markdown
# .claude/workflow.md

## 档位规则

### 自动判断规则
1. 如果变更涉及文件 < 5 个 AND 测试覆盖该模块 → 档位 A
2. 如果变更涉及文件 5-20 个 AND 不修改公共接口 → 档位 B
3. 如果变更涉及文件 > 20 个 OR 修改公共接口 → 档位 C
4. 如果是紧急 bug 修复（标记 [URGENT]）→ 档位 EMERGENCY

### 档位定义
- 档位 A（轻量）：直接编码，CI 验证
- 档位 B（标准）：propose → apply → archive
- 档位 C（重量）：propose → design → apply → verify → archive
- EMERGENCY：fix → 事后补 propose
```

**价值**：让流程的"重量"匹配变更的"重量"。

---

### 设计模式 2：失败模式的预设

**问题**：现有框架描述"应该怎样"，但很少描述"如果失败了怎么办"。

**设计**：每个外化决策都包含"反例"。

**示例**：

```markdown
# constitution.md

## 数据库使用约定

### 应该
- 所有 schema 变更通过 migration
- 所有查询走 ORM

### 不应该（明确反模式）
- ❌ 直接写 raw SQL（除非性能瓶颈，且需要单独审批）
- ❌ 在生产数据库做 ALTER TABLE
- ❌ 跨服务直接连数据库

### 失败时怎么办
- 发现违反约定的代码：先 alert 但不阻止合并
- 第二次发现同类违反：阻止合并并通知架构师
```

**价值**：让规范包含"边界条件"，更接近真实。

---

### 设计模式 3：决策的"半衰期"标注

**问题**：决策会过时，但现有框架没有提示机制。

**设计**：每个外化决策标注"半衰期"和"复审日期"。

**示例**：

```markdown
# decisions/2026-01-use-postgres.md

## 决策：使用 PostgreSQL 作为主数据库

**生效日期**：2026-01-15
**预计有效期**：18 个月
**复审日期**：2027-07-15
**触发复审条件**：
- 数据量 > 10 TB
- 写入 QPS > 50K
- 出现 PG 不擅长的场景（图查询、时序）

## 当前理由
- 团队熟悉
- 事务支持
- 数据量预估 < 1 TB

## 复审时考虑
- 是否仍满足上述假设？
- 替代方案是否成熟？
- 迁移成本是否可承受？
```

**价值**：避免"决策化石"——一个 5 年前的决策一直在影响今天。

---

### 设计模式 4：上下文加载的"层级缓存"

**问题**：每个任务都重新加载完整规范，浪费 token。

**设计**：把上下文分层，越内层越频繁加载。

**示例**：

```
Layer 1（每次会话必加载，~ 1K tokens）：
  - CLAUDE.md 摘要

Layer 2（涉及对应模块时加载，~ 5K tokens）：
  - 模块的 spec 摘要

Layer 3（具体任务时加载，~ 2K tokens）：
  - 任务相关的代码文件

Layer 4（按需加载，~ 不限）：
  - 完整规范、详细历史
```

**价值**：通过分层让"重要的常驻、不重要的按需"。

---

### 设计模式 5：决策的"反向表达"

**问题**：很多决策是"做什么"，但 AI 经常违反"不做什么"。

**设计**：把"禁止"和"应该"等量描述。

**示例**：

```markdown
# api-design.md

## API 设计准则

### 应该（引导）
- RESTful 风格
- 返回格式 { data, error, meta }
- 列表必须分页

### 禁止（约束，重要！）
- ❌ 不要用 GET 做修改操作
- ❌ 不要返回 200 OK 但 body 里报错
- ❌ 不要在 URL 暴露内部 ID
- ❌ 不要让客户端拼 SQL（即使是 GraphQL 风格的过滤）
```

**价值**：明确的禁止比模糊的引导更有效。

---

### 设计模式 6：Harness 的自我反思

**问题**：Harness 本身需要演进，但很少有项目主动反思 Harness 的有效性。

**设计**：建立 Harness 的元 Harness——定期评估并迭代。

**实现**：

```markdown
# .claude/harness-retrospect.md

## 每月反思（自动提醒）

### 数据
- 本月变更数：X
- 走完整流程的：X
- 跳过流程的：X
- 跳过的原因分布：[]

### 信号
- 哪些环节经常被跳过？（说明流程有摩擦）
- 哪些规范从未被引用？（可能可以删除）
- 哪些问题反复出现？（说明 Harness 没覆盖到）

### 行动
- 简化：[]
- 强化：[]
- 删除：[]
- 新增：[]
```

**价值**：Harness 不是一次定稿，是持续演进的活体。

---

## 第六部分：你的定制 Harness 设计模板

把前面所有内容浓缩为一个可执行的设计模板。

---

### 设计文档模板

```markdown
# 项目 Harness 设计文档

## 一、现状摘要
（基于第二部分的 6 大维度评估，给出关键信息）

- 项目阶段：
- 团队规模：
- 工具栈：
- 关键约束：

## 二、核心矛盾
（识别 2-3 个最关键的矛盾点）

矛盾 1：
矛盾 2：
矛盾 3：

## 三、设计目标
（基于核心矛盾，定义 Harness 要解决的问题）

目标 1：
目标 2：
目标 3：

## 四、技术选择

### 决策外化
- 文件格式：
- 文件粒度：
- 存储位置：
- 更新模型：
- 层次划分：

### 流程结构化
- 阶段数量：
- 门控类型：
- 是否多档位：
- 阶段定义：

### 任务原子化
- 粒度：
- 依赖管理：
- 执行模型：
- 上下文策略：
- 验证策略：

## 五、文件清单
（外化的所有文件及其作用）

| 文件 | 作用 | 维护者 | 更新频率 |
|------|------|--------|---------|
| | | | |

## 六、流程清单
（所有定义的流程及其档位）

| 流程 | 适用场景 | 阶段 | 验收标准 |
|------|---------|------|---------|
| | | | |

## 七、自动化清单
（所有 Hook 和自动化脚本）

| 触发时机 | 自动执行 | 失败行为 |
|---------|---------|---------|
| | | |

## 八、反思机制
- 评估周期：
- 评估指标：
- 调整流程：

## 九、演进路线
- Phase 1（当前）：
- Phase 2（3 个月后）：
- Phase 3（6 个月后）：
```

---

## 第七部分：关键启发与思维转换

### 思维转换 1：从"工具"到"设计空间"

**初学者思维**：哪个工具最好？我该用哪个？

**进阶思维**：每个工具是设计空间的一个点。我的项目应该落在空间的哪个位置？

**核心区别**：前者是"选择题"，后者是"应用题"。

---

### 思维转换 2：从"全部上"到"最小够用"

**初学者思维**：把所有最佳实践都用上才完整。

**进阶思维**：只引入解决"已经发生的问题"的能力。每个新增的 Harness 元素必须被痛点证明。

**核心原则**：YAGNI（You Aren't Gonna Need It）原则在 Harness 设计中同样适用。

---

### 思维转换 3：从"框架"到"原理"

**初学者思维**：模仿框架的做法。

**进阶思维**：理解框架背后的原理，重新组合元素。

**核心区别**：前者是"复制"，后者是"创造"。

---

### 思维转换 4：从"静态"到"演进"

**初学者思维**：设计一次性 Harness。

**进阶思维**：Harness 是项目的活体，必须随项目演进。

**核心原则**：保留 Harness 的"可改性"——任何决策都应该可以被未来的自己挑战。

---

### 思维转换 5：从"遵循"到"反思"

**初学者思维**：严格遵循框架的规定。

**进阶思维**：定期反思每条规定是否仍然有效，敢于删除过时的约束。

**核心原则**：Harness 不是教条，是为项目服务的。当 Harness 拖累项目时，是 Harness 该改，不是项目该改。

---

## 第八部分：终极原则——少即是多

整个 Harness Engineering 领域有一个隐含的规律：

> **真正高效的 Harness，往往是最简单的那个。**

为什么？因为：

1. **复杂的 Harness 会被绕过**——团队会找捷径
2. **复杂的 Harness 会过时更快**——元素越多，过时风险越高
3. **复杂的 Harness 维护成本高**——可能维护成本超过收益
4. **复杂的 Harness 屏蔽思考**——开发者只是"按规定做"，失去判断力

**终极原则**：

> **在能解决问题的前提下，选择元素最少、约束最弱、维护最低的 Harness。**

这不是"反对工程化"——而是**真正的工程化**：用最小的代价解决核心问题。

---

## 结语：你已经准备好了

学到这里，你已经具备：

- ✅ 看穿表象的能力（理解三大原理）
- ✅ 识别局限的能力（每个框架的边界）
- ✅ 评估现状的能力（6 大维度）
- ✅ 做出选择的能力（设计空间）
- ✅ 持续演进的能力（反思机制）

**剩下的就是动手了。**

不要纠结"我设计的对不对"——因为没有对错，只有适配与否。

不要追求"完美的 Harness"——因为完美是流动的，今天的完美是明天的负担。

**最好的 Harness 不是别人给你的，是你为自己设计的。**

每个真实的项目都有它独特的现实。每个真实的团队都有它独特的文化。每个真实的工具都有它独特的能力边界。

你的工作不是套用别人的答案——而是在这些约束中找到**属于你的最优解**。

---