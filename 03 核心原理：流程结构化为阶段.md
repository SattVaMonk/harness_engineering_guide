# 核心原理：流程结构化为阶段

## 一句话总结

> **流程结构化为阶段 = 给 LLM 装上"思维档位"。它把一团混沌的"端到端编程"切分成职责清晰、产物明确、可验收的有序阶段，每个阶段强制 AI 只关注当下，从根本上解决了"AI 一上来就写代码"的草率问题。**

如果说"决策外化为文件"解决的是**空间维度**（信息存哪里），那么"流程结构化为阶段"解决的是**时间维度**（什么时候做什么）。

---

## 一、问题的本质：LLM 倾向于"立刻产出"

### LLM 的一个反直觉特性

LLM 经过 RLHF 训练后，被强烈地塑造成一种**讨好型助手**——它倾向于尽快给出"看起来有用"的回答。这导致两个核心问题：

### 问题 1：跳过思考直接编码（Premature Implementation）

**症状**：
```
用户："给我做一个用户登录功能"
AI（3 秒后）：开始写 LoginController.ts、LoginService.ts、login.html...
```

**根因**：AI 没有问"用什么认证方式？JWT 还是 Session？要不要支持第三方登录？密码强度策略？" 直接基于训练数据中的常见模式开始编码。

结果：写出来的代码可能完全不符合项目实际需求，需要大量返工。

### 问题 2：决策与执行混杂（Mixed Concerns）

**症状**：在一次对话中，AI 同时在做四件事：
- 理解需求
- 设计架构
- 拆解任务
- 编写代码

**根因**：所有决策都在同一个上下文中并行发生，相互污染。

**典型表现**：
- 写代码时突然意识到架构有问题，回头改架构
- 改架构时发现需求理解错了，回头改需求
- 改完需求又导致已经写好的代码作废
- 一次对话产出 1000 行代码，其中 600 行是反复推翻重写的

### 问题 3：缺乏验收节点（No Checkpoints）

**症状**：用户从头到尾只在最后看到"完成的代码"，无法在中途纠偏。等发现 AI 走错方向时，已经浪费了大量 token 和时间。

---

## 二、原理：用阶段切分驱动渐进式收敛

### 核心机制

把"从需求到代码"这个连续过程**切分为有序的离散阶段**，每个阶段：

1. **职责单一**：只做一件事
2. **产物明确**：必须输出特定文档
3. **强制验收**：人类批准后才能进入下一阶段
4. **上下文聚焦**：当前阶段只加载相关信息

### 经典五阶段模型（Spec-Kit 风格）

```
┌──────────────┐  ┌──────────┐  ┌────────┐  ┌───────┐  ┌──────────┐
│ Constitution │→ │ Specify  │→ │  Plan  │→ │ Tasks │→ │Implement │
│   (宪法)     │  │  (需求)  │  │ (设计) │  │(拆解) │  │  (执行)  │
└──────────────┘  └──────────┘  └────────┘  └───────┘  └──────────┘
   全局约束        What/Why    技术方案    任务清单     代码产出
```

每个阶段的输入和产出严格定义：

| 阶段 | 输入 | 产出 | 关注点 |
|------|------|------|--------|
| Constitution | 项目目标 | constitution.md | 不可妥协的原则 |
| Specify | constitution.md | spec.md | 用户需求（What） |
| Plan | spec.md | plan.md | 技术方案（How） |
| Tasks | plan.md | tasks.md | 可执行单元 |
| Implement | tasks.md + 上述全部 | 代码 | 严格按任务实现 |

### 这本质上是什么？

**这是把"编程"从一个艺术活动，改造成了一条流水线**：

| 维度 | 端到端对话式编程 | 结构化阶段式编程 |
|------|-----------------|-----------------|
| **类比** | 手工艺人 | 工业流水线 |
| **决策时机** | 任何时刻都可能改 | 每阶段决策一次后冻结 |
| **可观察性** | 黑盒，最后才见结果 | 每阶段都有产出可审 |
| **错误成本** | 后期发现要全推倒 | 早期阶段就能拦截 |
| **协作方式** | AI 单干 | 人在每阶段把关 |

---

## 三、阶段切分的三个核心原则

### 原则 1：先 What 后 How（需求与技术分离）

**为什么重要？**

需求（What）和技术（How）混在一起讨论时，会出现：
- 用户说"我要个高性能搜索"
- AI 立刻想到"用 Elasticsearch + Redis 缓存"
- 但其实用户的"高性能"= 1 秒内返回，PostgreSQL 全文索引已经够了

**阶段切分的价值**：Specify 阶段强制只描述"做什么、为什么"，禁止讨论"用什么技术"。这迫使所有人（包括 AI）先达成对**问题**的共识，再讨论**解法**。

### 原则 2：先全局后局部（架构先于实现）

**为什么重要？**

如果 AI 一上来就写第一个文件，它对"系统全貌"是无知的——
- 不知道还有哪些模块要写
- 不知道模块间如何交互
- 不知道哪些是核心、哪些是辅助

结果：第一个文件写得"局部最优"，但拼起来"全局糟糕"。

**阶段切分的价值**：Plan 阶段强制 AI 先画完整架构图，再进入 Implement。这保证了局部决策都在全局视野下做出。

### 原则 3：先拆解后执行（任务清晰先于编码）

**为什么重要？**

直接让 AI "实现需求 X"，AI 会自己内部拆解，但这个拆解：
- 用户看不见，无法干预
- 拆解过程消耗 token，挤占编码上下文
- 拆解错了就只能整体重做

**阶段切分的价值**：Tasks 阶段把拆解显式化为 `tasks.md`，每条任务可独立审查、可独立执行、可独立回滚。

---

## 四、为什么"分阶段"比"一气呵成"更好？

很多人会问：分阶段不是变慢了吗？为什么不直接一次到位？

**核心原因**——三个根本优势：

### 1. 早期错误的纠错成本更低

软件工程经典理论：**需求阶段发现的 bug 修复成本是编码阶段发现的 1/100**。

阶段切分让错误**尽可能早被发现**：
```
Specify 阶段发现需求误解 → 改 1 句话
Plan 阶段发现架构错误   → 改 1 张图
Tasks 阶段发现任务遗漏  → 改 1 个 checkbox
Implement 阶段才发现   → 改 500 行代码
```

### 2. 每阶段的上下文更聚焦

LLM 在 Specify 阶段时，只需要思考"用户要什么"，不需要分心想"该用哪个数据库"。

**注意力是有限资源**——阶段切分把它集中到当前真正重要的事情上。

### 3. 每阶段都是天然的人机协作点

```
AI 完成 spec.md → 人类审阅，确认需求理解正确 → AI 进入 Plan
AI 完成 plan.md → 人类审阅，确认架构合理     → AI 进入 Tasks
AI 完成 tasks.md → 人类审阅，确认拆解恰当    → AI 开始 Implement
```

这种**门控（Gating）机制**让人类在每个关键决策点都能介入，避免 AI 跑偏太远。

---

## 五、各 Harness 框架的阶段设计对比

| 框架 | 阶段数 | 阶段名 | 设计哲学 |
|------|--------|--------|----------|
| **Spec-Kit** | 5 | Constitution → Specify → Plan → Tasks → Implement | 完整 SDLC，从原则到代码 |
| **OpenSpec** | 3 | Propose → Apply → Archive | 变更生命周期，强调审计 |
| **GSD** | 5 | Discuss → Plan → Execute → Verify → Ship | 加入验证和交付，工业流水线 |
| **Kiro** | 3 | Requirements → Design → Tasks | 极简版，AWS 内置 IDE 流程 |

### 关键观察

**最少 3 阶段，最多 5 阶段**——这不是巧合：
- 少于 3 阶段：无法有效分离"想清楚-设计好-做出来"
- 多于 5 阶段：阶段切换成本超过收益

**每个阶段都有强制产物**——这是阶段化的核心保障。没有产物就没有阶段，只是名义上的"切分"。

---

## 六、阶段切分的反模式

### 反模式 1：跳过阶段

**典型表现**：用户嫌 Specify 麻烦，直接说"快速搞一下"。

**后果**：回退到端到端编程，所有阶段化收益全部失去。

**正确做法**：对小修改可以**简化**阶段（合并 Plan 和 Tasks），但**不能跳过**。哪怕只是一行文字描述需求，也要写下来。

### 反模式 2:阶段间产物不传递

**典型表现**：完成 spec.md 后，开始 Plan 时 AI 没有读取 spec.md，凭对话记忆自由发挥。

**后果**：阶段化变成形式主义，决策仍在对话中飘忽。

**正确做法**：每个阶段必须**显式读取**前一阶段的产物。Spec-Kit 的 `/speckit.plan` 命令会自动加载 spec.md。

### 反模式 3:阶段内职责蔓延

**典型表现**：Plan 阶段开始写代码，Implement 阶段又回头改架构。

**后果**：阶段边界模糊，又退化为混沌。

**正确做法**：每个阶段严格遵守职责。Implement 阶段发现架构问题，**必须先回到 Plan 阶段修改**，而不是就地修。

---

## 七、延伸思考

### 与软件工程瀑布模型的关系

**有人会问**：阶段化不就是瀑布模型吗？瀑布模型不是被淘汰了吗？

**关键区别**：
| 维度 | 经典瀑布模型 | Harness 阶段化 |
|------|-------------|---------------|
| 阶段时长 | 月或季度 | 分钟或小时 |
| 回退成本 | 极高 | 极低（重写一个 md 文件） |
| 阶段间反馈 | 文档式交付 | 实时可验证 |
| 适用粒度 | 整个项目 | 每个功能/变更 |

阶段化不是回到瀑布，而是把瀑布的**严谨性**与敏捷的**快速迭代**结合——**微缩瀑布**（Micro-Waterfall）。

### 与决策外化的协同关系

```
决策外化为文件   → 提供"存储介质"
流程结构化为阶段 → 提供"产生节奏"
```

没有阶段化，决策外化就缺乏自然的产生时机；
没有决策外化，阶段化的产物无处落地。

两者是**结构与流程的协同**——结构（文件）承载流程（阶段）的产物，流程驱动结构的演进。

### 与"任务原子化"的衔接

阶段化把"一个功能"切分到 Tasks 阶段产出 `tasks.md`，每条任务是阶段化的**最小单元**。
而下一步的"任务原子化"，是把每条任务**独立到隔离的执行环境**中——这是另一篇文档要展开的内容。

---

## 八、一句话归纳每个原则

| 原则 | 一句话精髓 |
|------|-----------|
| 先 What 后 How | 不让 AI 用技术自嗨绑架需求 |
| 先全局后局部 | 不让 AI 在无知中写第一行代码 |
| 先拆解后执行 | 不让 AI 在脑内拆解后甩代码给你 |
| 强制阶段产物 | 不让阶段化沦为形式主义 |
| 阶段间显式传递 | 不让上下文成为决策的唯一载体 |

---

## 参考资源

- [GitHub 官方仓库 - spec-kit](https://github.com/github/spec-kit)
- [GSD 框架实战：解决 AI 编程的 context rot 问题](https://blog.zhijun.io/posts/gsd-get-shit-done-project-framework)
- [掘金 - 从 Vibe Coding 到 Spec-Driven：AI 编程范式的下一次进化](https://juejin.cn/post/7627129629574266934)
- [掘金 - 6 个代表性 AI 编程 Harness 工程化框架拆解](https://article.juejin.cn/post/7626998085928189998)
- [Boehm 软件工程经济学 - 缺陷成本曲线](https://en.wikipedia.org/wiki/Software_Engineering_Economics)